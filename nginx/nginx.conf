events {}

http {

    upstream frontend {
        server frontend:5173;
    }

    upstream api {
        server api:8000;
    }

    server {

        listen 80;

        location / {
            proxy_pass http://frontend;
        }

        location /api/ {
            proxy_pass http://api;
        }

    }
}